// Top-level build file
buildscript {
    repositories {
        flatDir {
            dirs 'libs'
        }
    }

    dependencies {
        classpath "com.android.tools.build:gradle:8.7.0"
    }
}

allprojects {
}